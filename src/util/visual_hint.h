#ifndef __MINI16_UTIL_VISUAL_HINT
#define __MINI16_UTIL_VISUAL_HINT

// NOTE: these macros mostly do nothing; they're here simply for
// the ease of code management.

// NOTE: when the function returns non-zero value, the operation has failed.
#define FAILURE_AT_NON_ZERO

// NOTE: when the function returns 0, the operation has failed.
#define FAILURE_AT_ZERO

// NOTE: when the function returns -1, the operation has failed.
#define FAILURE_AT_NEGATIVE_ONE

#endif
